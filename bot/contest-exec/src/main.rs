fn main() {
    println!("Hi!");

    // TODO: at some point, set some selinux bits to lock this down more

    // validate user #, add to base number
    // setuid/gid to user (panic on failure)
    // mkdtemp (panic on failure)
    // chdir (panic on failure)
    // run app
    // delete temp dir

}
